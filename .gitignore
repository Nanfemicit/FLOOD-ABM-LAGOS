# Python
__pycache__/
*.pyc
.ipynb_checkpoints/

# Local caches of raw downloaded source files (full global/regional extents
# before clipping to Lagos) — regenerable via the scripts/fetch_*.py scripts,
# not meant to be committed.
data/raw/**/_tile_cache/
data/raw/**/_source_cache/

# Local env
.venv/
.env

# Working notes -- Claude Code session context, not meant to be tracked
# (was briefly committed 6265ed6..b4feda6; untracked via git rm --cached,
# left in local history rather than rewritten -- see commit message)
CLAUDE.md
